# Optional — if unset, /api/insights falls back to deterministic commentary
# generated from the same dataset, so the app works fully without a key.
ANTHROPIC_API_KEY=

# Optional — defaults to a local SQLite file if unset
DATABASE_URL=sqlite:///./senus.db
